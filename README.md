# Платформа волонтерських проєктів

Моно-репозиторій для повнофункціональної платформи волонтерської координації в Україні. Проєкт поєднує сучасний UX, надійний бекенд і мобільний фронтенд, щоб координатори, волонтери та отримувачі допомоги взаємодіяли без затримок.

## Основні можливості (MVP)
- **JWT-аутентифікація** з ролями (`volunteer`, `coordinator`, `beneficiary`, `admin`) і персональним дашбордом.
- **Кампанії**: каталог, детальна сторінка, таймлайн етапів, заявки волонтерів, аналітика координаторів.
- **Зміни (shifts)**: планування координаторами, запис волонтерів, контроль місткості, швидке скасування.
- **UX фідбек**: глобальні toast-повідомлення, підтвердження критичних дій через модальні вікна (Radix UI), mobile-first адаптивність.
- **PWA-ready фронтенд** (Next.js 14 App Router, TailwindCSS, a11y-патерни).
- **Готовність до інтеграцій**: Monobank вебхуки, Redis, Celery, WebSocket-канали (передбачено в архітектурі).

## Технологічний стек
- **Backend**: Python 3.12, Django 5, DRF, PostgreSQL, Redis, Celery, SimpleJWT.
- **Frontend**: Next.js 14 (TypeScript), TailwindCSS, Radix UI, Jotai (стан), React Query (дані).
- **Інфраструктура**: Docker Compose (backend, frontend, db, redis, celery-worker), start-скрипт для Windows, `.env` через `infra/`.
- **Тестування**: `pytest` (API), Playwright e2e (волонтер → координатор → зміна), `npm run lint/build`.

## Структура репозиторію
```
backend/    # Django-проєкт (core, accounts, campaigns, payments)
frontend/   # Next.js App Router, компоненти, сторінки, дизайн-система
infra/      # .env.example, інфраструктурні налаштування
docs/       # План доставлення, архітектурні матеріали
start-project.bat # Windows-скрипт для швидкого запуску середовища
docker-compose.yml
```

## Швидкий старт
> **Вимоги:** Docker Desktop (із запущеним Docker Engine), Node.js 20+, npm 10+, Git.

1. Клонувати репозиторій та перейти в корінь:
   ```powershell
   git clone <repo-url>
   cd <repo-folder>
   ```
2. Скопіювати змінні оточення:
   ```powershell
   copy infra\.env.example infra\.env
   ```
3. На Windows запустити:
   ```powershell
   .\start-project.bat
   ```
   Скрипт перевірить Docker, створить `.env`, збілдить і підніме всі сервіси (`backend`, `frontend`, `db`, `redis`, `celery-worker`).
4. Для ручного запуску (будь-яка ОС):
   ```bash
   docker compose up --build
   ```

Після успішного старту:
- Backend API: http://localhost:8000/api/v1/
- Frontend UI: http://localhost:3000/

## Скріпти розробника
```bash
# фронтенд
cd frontend
npm install
npm run lint
npm run build
npx playwright install --with-deps
npm run test:e2e -- --project=chromium-desktop

# бекенд (усередині контейнера backend)
python manage.py test campaigns
```

> **Порада.** Щоб прогнати e2e та одночасно підняти дев-сервер фронтенду, встановіть змінні середовища перед запуском:
> ```powershell
> $env:PLAYWRIGHT_WEB_SERVER_CMD = "npm run dev"
> $env:PLAYWRIGHT_BASE_URL = "http://127.0.0.1:3000"
> $env:PLAYWRIGHT_API_URL = "http://127.0.0.1:8000/api/v1"
> npm run test:e2e
> ```

## CI та автоматичні перевірки
- **Frontend E2E** (GitHub Actions): на гілках `main` і pull request проганяє Playwright-сценарій «волонтер → координатор → зміна → вихід зі зміни». Розгортання включає міграції Django, запуск локального бекенда та дев-сервера Next.js. Звіти Playwright додаються артефактами у разі падіння.

## Поточний статус
- ✅ Реалізовано: аутентифікація, заявки кампаній, координаторські панелі, запис волонтера на зміну, UX-повідомлення, Playwright e2e-сценарій «волонтер → координатор → зміна → вихід зі зміни», GitHub Actions workflow для e2e.
- 🔄 У процесі: документування додаткових сценаріїв, розширення e2e-набору, автоматизація аналітичних тестів.
- 🔜 Планується: push/e-mail сповіщення, розширена аналітика, інтеграція Monobank Webhook, PWA offline cache.

## Безпека та a11y
- RBAC контролює API доступ; усі критичні операції підтверджуються на фронтенді.
- Семантичні теги (`<main>`, `<nav>`, `<button>`) + aria-атрибути + focus-states.
- Санітизація даних через DRF серіалізатори і фільтрацію запитів.

## Ліцензія
MIT (можна адаптувати під потреби організації).

---
Якщо потрібна інтеграція додаткових сервісів (SMS, CRM, BI) — дивіться `docs/delivery-plan.md` для розширеної дорожньої карти.
