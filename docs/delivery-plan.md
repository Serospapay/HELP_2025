/**
 * @file: delivery-plan.md
 * @description: Покроковий план реалізації платформи волонтерських проєктів з архітектурою та критеріями прийняття.
 * @dependencies: N/A
 * @created: 2025-11-08
 */

# План реалізації платформи волонтерських проєктів

## 1. Загальна концепція
- **Мета**: забезпечити прозору взаємодію волонтерів, координаторів і отримувачів допомоги.
- **Технології**: Backend — Django + DRF; Frontend — Next.js (TS); БД — PostgreSQL; кеш — Redis; черги — Celery + Redis; CI/CD — GitHub Actions.
- **Інтеграції**: Платежі Monobank/Privat24, e-mail (SendGrid), push-сповіщення (Firebase), карти (Mapbox/Leaflet), геокодування (Nominatim).

## 1.1 Поточний прогрес (листопад 2025)
- ✔️ **Аутентифікація та ролі**: Custom `User`, JWT (login/register/refresh), дашборди з персоналізацією.
- ✔️ **Модуль кампаній**: CRUD, фільтри, детальні сторінки, подання та модерація заявок, координаторська аналітика.
- ✔️ **Зміни та призначення**: Планування змін, запис/скасування волонтерів, валідація місткості, API `/api/v1/my-shift-assignments/`.
- ✔️ **UX-патерни**: глобальні toast-повідомлення, Radix AlertDialog для небезпечних дій, mobile-first компоненти.
- ✔️ **Документація та e2e**: README оновлено, Playwright-сценарій «волонтер → координатор → зміна» автоматизовано з підготовкою/очисткою даних.
- ⏳ **Додаткові гіди**: деталізація delivery-plan, опис UX-патернів для координаторів та аналітики.
- 🔜 **Інтеграції Monobank / сповіщення**: вебхуки, push/e-mail, KPI dashboards.

## 2. Архітектура

### 2.1 Backend (Django + DRF)
- **Сервіси**: `auth`, `campaigns`, `tasks`, `beneficiaries`, `donations`, `notifications`, `analytics`.
- **ORM моделі**:
  - `User` (Custom) з ролями `volunteer`, `coordinator`, `beneficiary`, `admin`.
  - `VolunteerProfile`, `CoordinatorProfile`, `BeneficiaryProfile`.
  - `Campaign`, `CampaignStage`, `CampaignMedia`.
  - `Task`, `TaskAssignment`, `Shift`.
  - `BeneficiaryRequest`, `Delivery`.
  - `Donation`, `PaymentWebhook`.
  - `NotificationPreference`, `NotificationLog`.
- **API**: REST-first з OpenAPI документацією; версіяціювати `/api/v1`.
- **Безпека**: JWT + refresh, RBAC, валідація DRF serializers, rate limiting, audit log.

### 2.2 Frontend (Next.js + TypeScript)
- **Структура**: `app` router, layout для ролей, окремі маршрути `campaigns`, `dashboard`, `tasks`, `beneficiaries`.
- **Стан**: React Query для даних + Zustand для сесії/UX-станів.
- **UI**: TailwindCSS + Headless UI, власна дизайн-система (кнопки, карточки, модальні вікна, форми).
- **a11y**: семантичні теги, aria-атрибути, доступні компоненти, перевірка axe-core у pipeline.

### 2.3 Інфраструктура
- **Docker**: окремі контейнери `web`, `frontend`, `db`, `redis`, `celery`, `worker`.
- **CI/CD**: GitHub Actions (lint, tests, build, deploy), використання `.env` через GitHub Secrets.
- **Моніторинг**: Sentry (errors), Prometheus/Grafana (metrics), структуровані логи.

## 3. Беклог (High-level Roadmap)

### Етап 0 — Підготовка (2–3 дні)
- Створити репозиторій (monorepo).
- Налаштувати Docker Compose з `backend`, `frontend`, `db`, `redis`.
- Додати базові пайплайни lint/test.
- Критерій: `docker compose up` піднімає робоче середовище, тести `pytest` і `vitest` (stub) проходять.

### Етап 1 — Аутентифікація та ролі (5–7 днів)
- Реалізувати custom user model, JWT, реєстрацію, логін, відновлення паролю.
- Підтримка KYC для координаторів (форма + валідація документів, статуси).
- UI: форми реєстрації/логіну (mobile-first), перевірка помилок, повідомлення.
- Критерій: волонтер/координатор/отримувач можуть зареєструватися, увійти й бачити персоналізований dashboard (з заглушками).

### Етап 2 — Модуль кампаній (10–12 днів)
- Backend: CRUD `Campaign`, `CampaignStage`, медіа, пошук/фільтри.
- Frontend: каталог кампаній, фільтри, cards, detail page з таймлайном, заявка волонтера.
- Інтеграції карт: відображення локації кампанії, кластеризація.
- Критерій: координатор створює кампанію, волонтер знаходить та подає заявку, отримувач бачить статус доступних кампаній.

### Етап 3 — Завдання і логістика (8–10 днів)
- Backend: моделі `Task`, `Shift`, `TaskAssignment`, статуси виконання.
- Frontend: календар змін (agenda/kanban), підтвердження участі, трекінг доставки для отримувачів.
- Push/e-mail сповіщення про зміни.
- Критерій: коорд. планує зміни, волонтер підтверджує, отримувач бачить очікувану доставку.

### Етап 4 — Платежі та аналітика (7–9 днів)
- Вебхуки платіжних систем, відображення прогресу зборів у реальному часі.
- Аналітика: графіки KPI, експорт CSV, API для статистики.
- Критерій: кампанія отримує пожертви, сума оновлюється, координатор бачить звітність.

### Етап 5 — Полірування та реліз (5–7 днів)
- Тестування (unit, e2e з Playwright), ручні сценарії.
- Оптимізація продуктивності (lazy loading, code splitting).
- Документація (README, API docs, user guides).
- Критерій: пройдений чекліст тестів, опублікована документація, договірне середовище готове до деплою.

- **Unit**: `pytest`, `jest/vitest`.
- **Integration**: DRF APIClient, Playwright e2e (виконано сценарій «волонтер → координатор → зміна → вихід зі зміни» з API-підготовкою та очищенням даних).
- **CI**: GitHub Actions workflow `Frontend E2E` автоматично проганяє Playwright-тести на push/pull request до `main`, підіймаючи Django й Next.js у пайплайні та додаючи звіти як артефакти.
- **E2E backlog (поточний стан)**:
  1. ✅ Волонтер реєструється → подає заявку → координатор підтверджує → волонтер записується на зміну → виходить зі зміни (автоматизовано Playwright-сценарієм + CI).
  2. ⏳ Координатор створює/редагує етапи та зміни → волонтер бачить оновлений розклад.
- **Security**: перевірка OWASP (CSRF, XSS, SQLi), тестування ролей.
- **Perf**: Lighthouse CI, k6 для бекенду.

## 5. Ризики та план мінімізації
- **Інтеграції банків**: невизначені SLA → абстракція через сервіс `payment-provider`, локальні моки.
- **Конфіденційні дані**: шифрування полів, обмеження доступу, аудит доступів.
- **Навантаження**: кешування гарячих даних (Redis), background jobs для вебхуків.
- **Безперервність**: резервні копії PostgreSQL, health-checks, failover стратегія.

## 6. Наступні кроки
1. Розширити користувацьку документацію (user flow діаграми, гайд для координаторів/волонтерів).
2. Додати нові e2e-сценарії (повторний вихід/повторне приєднання, управління етапами/змінами) та підключити їх до чинного CI.
3. Розпочати роботу над сповіщеннями (push/e-mail) та інтеграцією Monobank webhook.
4. Запланувати розширення аналітики (dashboard + auto-refresh) й підключити реальні платіжні дані.




